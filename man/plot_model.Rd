% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_model}
\alias{plot_model}
\title{Plot Fitted Models}
\usage{
plot_model(list, x_var, y_var, return_data = FALSE)
}
\arguments{
\item{list}{A list as produced by \code{fit_curve()}.}

\item{x_var}{Character string specifying the column name for the independent
variable (typically time).}

\item{y_var}{Character string specifying the column name for the dependent
variable (e.g., cell count, confluence, or a calculated metric).}

\item{return_data}{Logical indicating whether to return plot data instead of
the plot object (default: FALSE).}
}
\value{
A ggplot object (default) or a list containing original and predicted
data for external plotting (if return_data = TRUE).
}
\description{
Create publication-quality plots showing fitted model curves overlaid on
experimental data points, with support for grouped models and outliers.
}
\examples{
\dontrun{
# Plot single model fit
model_plot <- plot_model(
  data = growth_data,
  x_var = "time_hours",
  y_var = "cell_count",
  obj = fitted_model
)
print(model_plot)
# Return data for custom plotting
plot_data <- plot_model(
  data = growth_data,
  x_var = "time_hours",
  y_var = "cell_count",
  fit = fitted_model,
  return_data = TRUE
)
}
}
