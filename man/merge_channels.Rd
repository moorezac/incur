% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/python.R
\name{merge_channels}
\alias{merge_channels}
\title{Merge multi-channel time-series images}
\usage{
merge_channels(input_dirs, output_dir, cores = NULL, python = "python3")
}
\arguments{
\item{input_dirs}{Character vector of paths to input directories, one per
channel. All directories must contain identically named \code{.tif} files.}

\item{output_dir}{Character. Path to the directory where merged \code{.tif}
files will be saved.}

\item{cores}{Integer. Number of CPU cores to use for parallel processing.
Defaults to \code{NULL} (all available cores).}

\item{python}{Character. Path to the Python executable. Defaults to
\code{"python3"}.}
}
\value{
The stdout/stderr output from the Python process, returned
invisibly. Called for its side effect of writing merged TIFFs to
\code{output_dir}.
}
\description{
Combines corresponding \code{.tif} files from multiple input directories
(one per channel) into a single multi-channel time-series TIFF. Frames are
aligned by acquisition timestamp, converted to 8-bit, and stacked along the
channel axis.
}
\examples{
\dontrun{
merge_channels(
  input_dirs = c("/data/channel1", "/data/channel2"),
  output_dir = "/data/merged"
)

# Limit CPU usage
merge_channels(
  input_dirs = c("/data/ch1", "/data/ch2", "/data/ch3"),
  output_dir = "/data/merged",
  cores      = 4
)
}

}
