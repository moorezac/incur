% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/python.R
\name{run_trackmate}
\alias{run_trackmate}
\title{Run TrackMate cell tracking on segmented images}
\usage{
run_trackmate(
  input_dir,
  fiji_path,
  verbose = FALSE,
  target_channel = 1,
  simplify_contours = TRUE,
  quality_threshold = 0,
  allow_frame_linking = TRUE,
  frame_linking_max_distance = 200,
  allow_gap_closing = TRUE,
  gap_closing_max_distance = 200,
  gap_closing_max_frame_gap = 2L,
  allow_track_splitting = TRUE,
  track_splitting_max_distance = 15,
  allow_track_merging = FALSE,
  track_merging_max_distance = 15,
  python = "python3"
)
}
\arguments{
\item{input_dir}{Character. Path to the directory containing \code{.tif}
images and their corresponding \code{_cp_masks.tif} Cellpose mask files.}

\item{fiji_path}{Character. Path to the Fiji/ImageJ executable (e.g.
\code{"/opt/fiji/Fiji.app/ImageJ-linux64"}).}

\item{verbose}{Logical. If \code{TRUE}, prints the TrackMate configuration
and the Fiji command before execution. Defaults to \code{FALSE}.}

\item{target_channel}{Integer. Channel index (1-based) used for spot
detection. Defaults to \code{1}.}

\item{simplify_contours}{Logical. Whether to simplify detected contours.
Defaults to \code{TRUE}.}

\item{quality_threshold}{Numeric. Minimum spot quality score; spots below
this value are filtered out. Defaults to \code{0.0}.}

\item{allow_frame_linking}{Logical. Whether to link spots between consecutive
frames. Defaults to \code{TRUE}.}

\item{frame_linking_max_distance}{Numeric. Maximum distance (in pixels) for
linking spots between frames. Defaults to \code{200.0}.}

\item{allow_gap_closing}{Logical. Whether to close gaps in tracks caused by
missed detections. Defaults to \code{TRUE}.}

\item{gap_closing_max_distance}{Numeric. Maximum distance (in pixels) for
gap closing. Defaults to \code{200.0}.}

\item{gap_closing_max_frame_gap}{Integer. Maximum number of frames over
which a gap can be closed. Defaults to \code{2}.}

\item{allow_track_splitting}{Logical. Whether to allow tracks to split (e.g.
cell division). Defaults to \code{TRUE}.}

\item{track_splitting_max_distance}{Numeric. Maximum distance (in pixels)
for a track split event. Defaults to \code{15.0}.}

\item{allow_track_merging}{Logical. Whether to allow tracks to merge.
Defaults to \code{FALSE}.}

\item{track_merging_max_distance}{Numeric. Maximum distance (in pixels) for
a track merge event. Defaults to \code{15.0}.}

\item{python}{Character. Path to the Python executable. Defaults to
\code{"python3"}.}
}
\value{
The stdout/stderr output from the Python process, returned
invisibly. Called for its side effect of writing TrackMate \code{.xml}
and \code{_spots.csv} files.
}
\description{
Merges Cellpose mask TIFFs with their corresponding images, generates a
TrackMate macro, and runs it via the Fiji/ImageJ CLI. Outputs per-image
\code{.xml} track files and \code{_spots.csv} tables to a \code{trackmate/}
subdirectory within \code{input_dir}.
}
\examples{
\dontrun{
# Basic usage with defaults
run_trackmate(
  input_dir = "/data/experiment1",
  fiji_path = "/opt/fiji/Fiji.app/ImageJ-linux64"
)

# Tighter linking distances, no splitting or merging
run_trackmate(
  input_dir                  = "/data/experiment1",
  fiji_path                  = "/opt/fiji/Fiji.app/ImageJ-linux64",
  frame_linking_max_distance = 100,
  gap_closing_max_frame_gap  = 3L,
  allow_track_splitting      = FALSE,
  allow_track_merging        = FALSE,
  verbose                    = TRUE
)
}

}
