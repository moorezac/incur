% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/python.R
\name{run_cellpose}
\alias{run_cellpose}
\title{Run Cellpose segmentation on a directory of images}
\usage{
run_cellpose(input_dir, channels = 1, cellpose_args = NULL, python = "python3")
}
\arguments{
\item{input_dir}{Character. Path to the directory containing \code{.tif}
files to segment.}

\item{channels}{Integer or integer vector. Channel(s) to use for
segmentation (1-based indexing). Defaults to \code{1}.}

\item{cellpose_args}{Character vector of additional arguments passed directly
to the Cellpose CLI (e.g. \code{c("--pretrained_model", "cyto3",
  "--diameter", "30")}). These are appended after \code{--} and passed
through verbatim. Defaults to \code{NULL}.}

\item{python}{Character. Path to the Python executable. Defaults to
\code{"python3"}.}
}
\value{
A character vector of stdout/stderr output from the Python process,
returned invisibly.
}
\description{
Calls the Cellpose segmentation pipeline on all \code{.tif} files in the
specified directory. Each image is split into individual frames, segmented,
and the resulting masks are stacked back into a time-series TIFF.
}
\examples{
\dontrun{
# Basic usage
run_cellpose("/data/experiment1")

# Multi-channel with custom Cellpose settings
run_cellpose(
  input_dir     = "/data/experiment1",
  channels      = c(1, 2),
  cellpose_args = c("--pretrained_model", "cyto3", "--diameter", "30")
)
}

}
